name: Build Musafir APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3

      - name: Setup Java 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Node.js 18
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Cordova
        run: npm install -g cordova@11

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK tools
        run: |
          sdkmanager "build-tools;33.0.2" "platforms;android-33"

      - name: Create Cordova Project
        run: |
          cordova create musafir_app com.musafir.game Musafir
          cd musafir_app
          cordova platform add android@11

      - name: Copy Game HTML
        run: cp game/index.html musafir_app/www/index.html

      - name: Write Cordova config
        run: |
          cat > musafir_app/config.xml << 'CFGEOF'
          <?xml version="1.0" encoding="utf-8"?>
          <widget id="com.musafir.game" version="1.0.0" xmlns="http://www.w3.org/ns/widgets" xmlns:cdv="http://cordova.apache.org/ns/1.0">
            <name>Musafir</name>
            <description>Road Adventure Game</description>
            <author>finalsoftwere-byte</author>
            <content src="index.html" />
            <allow-intent href="http://*/*" />
            <allow-intent href="https://*/*" />
            <preference name="android-minSdkVersion" value="21" />
            <preference name="android-targetSdkVersion" value="33" />
            <preference name="Fullscreen" value="true" />
            <preference name="Orientation" value="portrait" />
            <preference name="DisallowOverscroll" value="true" />
          </widget>
          CFGEOF

      - name: Build Debug APK
        run: |
          cd musafir_app
          cordova build android --debug 2>&1
          find . -name "*.apk" -type f

      - name: Copy APK
        run: |
          cp musafir_app/platforms/android/app/build/outputs/apk/debug/app-debug.apk Musafir_Game.apk
          echo "APK size: $(du -sh Musafir_Game.apk)"

      - name: Delete old release if exists
        run: gh release delete v1.0 --yes 2>/dev/null || true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload APK to GitHub Release
        run: gh release create v1.0 Musafir_Game.apk --title "Musafir Game v1.0" --notes "Download APK and install on Android!"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
